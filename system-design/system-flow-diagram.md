# TinyURL System Design - Complete Flow Diagrams

## 🔄 Complete System Flow

### 1. URL Shortening Request Flow

```
┌─────────────┐
│   Client    │
│  (Browser)  │
└──────┬──────┘
       │
       │ POST /api/shorten
       │ { longUrl: "https://..." }
       ▼
┌─────────────────────────────────────┐
│      Load Balancer                  │
│  (Nginx/HAProxy/AWS ELB)            │
└──────┬──────────────────┬──────────┘
       │                  │
       │ Round-robin      │
       ▼                  ▼
┌─────────────┐    ┌─────────────┐
│  Web Server │    │  Web Server │
│  Instance 1 │    │  Instance 2 │
└──────┬──────┘    └──────┬──────┘
       │                  │
       │                  │
       ▼                  ▼
┌─────────────────────────────────────┐
│    Application Server Layer         │
│                                     │
│  1. Validate URL format             │
│  2. Check for malicious URL         │
│  3. Rate limiting check             │
│  4. Check if URL exists in cache    │
│     ├─ If exists: Return existing   │
│     └─ If not: Continue            │
│  5. Generate unique short code     │
│     ├─ Method: Base62 encoding     │
│     └─ Counter-based (atomic)      │
│  6. Check uniqueness in DB          │
│  7. Store in Database               │
│  8. Store in Cache (Redis)          │
│  9. Return short URL                │
└──────┬──────────────────┬──────────┘
       │                  │
       │ Write            │ Write
       ▼                  ▼
┌─────────────┐    ┌─────────────┐
│   Redis     │    │  MongoDB    │
│   Cache     │    │  Database   │
│             │    │             │
│  Key: url:  │    │  Collection:│
│    abc123   │    │    urls     │
│  Value:     │    │             │
│  {original, │    │  Document:  │
│   expires}  │    │  {short_code│
│  TTL: 1 day │    │   original_ │
│             │    │   url, ...} │
└─────────────┘    └─────────────┘
```

### 2. URL Redirection Flow (Read Path)

```
┌─────────────┐
│   Client    │
│  (Browser)  │
└──────┬──────┘
       │
       │ GET /abc123
       ▼
┌─────────────────────────────────────┐
│      Load Balancer                  │
└──────┬──────────────────┬──────────┘
       │                  │
       ▼                  ▼
┌─────────────┐    ┌─────────────┐
│  Web Server │    │  Web Server │
└──────┬──────┘    └──────┬──────┘
       │                  │
       ▼                  ▼
┌─────────────────────────────────────┐
│    Application Server                │
│                                     │
│  1. Validate short code format      │
│  2. Check Redis Cache (L1)          │
│     ├─ Cache Hit:                   │
│     │   ├─ Check expiration         │
│     │   ├─ Update click count (async)│
│     │   ├─ Track analytics (async)  │
│     │   └─ Return 301 Redirect     │
│     │                               │
│     └─ Cache Miss:                  │
│         ├─ Check MongoDB (L2)       │
│         │   ├─ If found:            │
│         │   │   ├─ Check expiration │
│         │   │   ├─ Store in cache   │
│         │   │   ├─ Update click count│
│         │   │   ├─ Track analytics  │
│         │   │   └─ Return 301       │
│         │   │                       │
│         │   └─ If not found:        │
│         │       └─ Return 404       │
└──────┬──────────────────┬──────────┘
       │                  │
       │ Read             │ Read
       ▼                  ▼
┌─────────────┐    ┌─────────────┐
│   Redis     │    │  MongoDB    │
│   Cache     │    │  Database   │
│             │    │             │
│  Fast Path  │    │  Fallback   │
│  (< 5ms)    │    │  (< 50ms)   │
└─────────────┘    └─────────────┘
```

### 3. Database Sharding Strategy

```
┌─────────────────────────────────────────┐
│         Application Layer              │
│                                        │
│  Short Code: "abc123"                  │
│  First Char: 'a'                      │
│  Shard Key: 'a' → Shard 1             │
└──────────────┬─────────────────────────┘
               │
               │ Consistent Hashing
               ▼
┌─────────────────────────────────────────┐
│         Shard Router                    │
│  (Consistent Hash Ring)                 │
└──┬──┬──┬──┬──┬──┬──┬──┬──┬──┬──┬──┬──┬─┘
   │  │  │  │  │  │  │  │  │  │  │  │
   ▼  ▼  ▼  ▼  ▼  ▼  ▼  ▼  ▼  ▼  ▼  ▼
┌──┐┌──┐┌──┐┌──┐┌──┐┌──┐┌──┐┌──┐┌──┐┌──┐
│S0││S1││S2││S3││S4││S5││S6││S7││S8││S9│
│0-9││a-f││g-m││n-t││u-z││A-F││G-M││N-T││U-Z│
└──┘└──┘└──┘└──┘└──┘└──┘└──┘└──┘└──┘└──┘
   │  │  │  │  │  │  │  │  │  │  │  │
   └──┴──┴──┴──┴──┴──┴──┴──┴──┴──┴──┴──┘
       Each Shard = Separate Database
       Replication: Master-Slave per Shard
```

### 4. Caching Strategy (Multi-Level)

```
┌─────────────────────────────────────────┐
│         Request Flow                    │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│   Level 1: Application Memory Cache     │
│   (Local Cache - In-Memory)             │
│   - Size: 100MB per instance            │
│   - TTL: 5 minutes                      │
│   - Hit Rate: ~60%                      │
└──────────────┬──────────────────────────┘
               │ Cache Miss
               ▼
┌─────────────────────────────────────────┐
│   Level 2: Redis Cluster                │
│   (Distributed Cache)                   │
│   - Size: 1TB total                     │
│   - TTL: 1 day                          │
│   - Hit Rate: ~80%                      │
│   - Replication: 3 replicas              │
└──────────────┬──────────────────────────┘
               │ Cache Miss
               ▼
┌─────────────────────────────────────────┐
│   Level 3: Database (MongoDB)           │
│   - Master-Slave setup                  │
│   - Read from Slaves                    │
│   - Write to Master                     │
│   - Hit Rate: 100% (always available)   │
└─────────────────────────────────────────┘
```

### 5. Counter Generation Flow (Distributed)

```
┌─────────────────────────────────────────┐
│    Application Server 1                 │
│    Needs: Unique ID                    │
└──────────────┬──────────────────────────┘
               │
               │ Request Counter Range
               ▼
┌─────────────────────────────────────────┐
│    Counter Service                      │
│    (Zookeeper/etcd/Database)            │
│                                         │
│  Current Counter: 1,000,000            │
│  Allocate Range: 1,000,000 - 1,000,999 │
│  (1000 IDs per allocation)              │
└──────────────┬──────────────────────────┘
               │
               │ Return Range
               ▼
┌─────────────────────────────────────────┐
│    Application Server 1                 │
│    Local Counter: 1,000,000             │
│    Uses: 1,000,000, 1,000,001, ...     │
│    Until: 1,000,999                     │
│    Then: Request new range              │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│    Application Server 2                 │
│    Gets Range: 1,001,000 - 1,001,999   │
│    (No collision with Server 1)         │
└─────────────────────────────────────────┘
```

### 6. Analytics Tracking Flow

```
┌─────────────────────────────────────────┐
│    URL Redirect Request                 │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│    Application Server                   │
│                                         │
│  1. Redirect user (synchronous)         │
│  2. Queue analytics event (async)      │
└──────────────┬──────────────────────────┘
               │
               │ Async Queue
               ▼
┌─────────────────────────────────────────┐
│    Message Queue                        │
│    (RabbitMQ/Kafka/Redis Queue)         │
│                                         │
│  Event: {                               │
│    shortCode: "abc123",                 │
│    timestamp: "2024-01-15T10:30:00Z",  │
│    ip: "192.168.1.1",                   │
│    userAgent: "Chrome/...",             │
│    country: "US"                        │
│  }                                      │
└──────────────┬──────────────────────────┘
               │
               │ Consumer Workers
               ▼
┌─────────────────────────────────────────┐
│    Analytics Processors                 │
│                                         │
│  1. Validate event                      │
│  2. Enrich with geo data                │
│  3. Store in analytics DB               │
│  4. Update aggregate stats              │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│    Analytics Database                   │
│    (Time-series DB: InfluxDB/Timescale) │
│                                         │
│  - Click events                         │
│  - Aggregate statistics                 │
│  - Geographic distribution              │
│  - Time-based trends                    │
└─────────────────────────────────────────┘
```

### 7. Rate Limiting Flow

```
┌─────────────────────────────────────────┐
│    Client Request                       │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│    Rate Limiter Middleware              │
│                                         │
│  1. Extract identifier (IP/User ID)     │
│  2. Check Redis for current count       │
│     Key: "rate_limit:IP:endpoint"       │
│     Value: { count: 45, window: 3600 } │
│                                         │
│  3. Check limit:                        │
│     ├─ If count < limit:                │
│     │   ├─ Increment count              │
│     │   └─ Allow request                │
│     │                                   │
│     └─ If count >= limit:               │
│         └─ Return 429 (Too Many Requests)│
└──────────────┬──────────────────────────┘
               │
               │ Token Bucket Algorithm
               ▼
┌─────────────────────────────────────────┐
│    Redis Cache                          │
│                                         │
│  Key: "rate_limit:192.168.1.1:/api/shorten"│
│  Value: {                               │
│    tokens: 55,                          │
│    lastRefill: timestamp,               │
│    capacity: 100,                       │
│    refillRate: 100/hour                 │
│  }                                      │
└─────────────────────────────────────────┘
```

### 8. Expiration Cleanup Flow

```
┌─────────────────────────────────────────┐
│    Cron Job (Every Hour)                │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│    Cleanup Service                      │
│                                         │
│  1. Query expired URLs:                 │
│     WHERE expires_at < NOW()            │
│                                         │
│  2. Batch delete (1000 at a time)      │
│                                         │
│  3. Remove from cache:                  │
│     DEL url:abc123                      │
│                                         │
│  4. Archive analytics (optional)        │
│                                         │
│  5. Log cleanup stats                   │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│    Database & Cache                     │
│                                         │
│  Deleted: 1,234 expired URLs            │
│  Cache cleared: 1,234 keys              │
│  Analytics archived: 1,234 records      │
└─────────────────────────────────────────┘
```

## 📊 Performance Metrics Flow

```
┌─────────────────────────────────────────┐
│    Application Servers                  │
│    (Instrumented with metrics)          │
└──────────────┬──────────────────────────┘
               │
               │ Metrics Export
               ▼
┌─────────────────────────────────────────┐
│    Metrics Collector                    │
│    (Prometheus/StatsD)                   │
│                                         │
│  - Request count                        │
│  - Response time (p50, p95, p99)       │
│  - Error rate                           │
│  - Cache hit rate                       │
│  - Database query time                  │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│    Monitoring Dashboard                 │
│    (Grafana/Datadog)                    │
│                                         │
│  - Real-time graphs                     │
│  - Alerts on thresholds                 │
│  - Performance trends                   │
└─────────────────────────────────────────┘
```

## 🔐 Security Flow

```
┌─────────────────────────────────────────┐
│    Incoming Request                     │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│    Security Middleware                  │
│                                         │
│  1. Input Validation                    │
│     ├─ URL format check                 │
│     ├─ Length validation                │
│     └─ Character validation             │
│                                         │
│  2. Malicious URL Detection             │
│     ├─ Blacklist check                  │
│     ├─ Phishing detection               │
│     └─ Malware scan                     │
│                                         │
│  3. Rate Limiting                       │
│     ├─ Per IP limit                     │
│     └─ Per user limit                   │
│                                         │
│  4. Authentication (if required)        │
│     ├─ API key validation               │
│     └─ JWT token validation             │
└──────────────┬──────────────────────────┘
               │
               ├─ Allowed: Continue
               │
               └─ Blocked: Return 403/429
```

## 🎯 Complete Request Lifecycle

```
Time: 0ms    ┌─ Client sends request
             │
Time: 5ms    ├─ Load balancer routes
             │
Time: 10ms   ├─ Rate limiter checks
             │
Time: 15ms   ├─ Security validation
             │
Time: 20ms   ├─ Cache lookup (Redis)
             │
Time: 25ms   ├─ Cache HIT: Return response
             │  OR
             │  Cache MISS: Continue
             │
Time: 50ms   ├─ Database query (MongoDB)
             │
Time: 70ms   ├─ Store in cache
             │
Time: 75ms   ├─ Update analytics (async)
             │
Time: 80ms   └─ Return response to client

Total: ~80ms (cache miss)
Total: ~25ms (cache hit)
```

---

## 📝 Key Takeaways

1. **Multi-level Caching**: Application → Redis → Database
2. **Async Processing**: Analytics aur updates async mein
3. **Sharding**: Database ko scale karne ke liye
4. **Rate Limiting**: Abuse prevention ke liye
5. **Monitoring**: Real-time metrics aur alerts
6. **Security**: Multiple layers of validation

---

*Complete system design flow for interview preparation*

